/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Api
 * NeuroScan brain tumor detection API
 * OpenAPI spec version: 0.1.0
 */
export interface HealthStatus {
  status: string;
}

export interface PredictRequest {
  /** Base64-encoded MRI scan image (without data URI prefix) */
  imageBase64: string;
  /** Original filename of the uploaded image */
  filename: string;
}

export interface ClassProbability {
  label: string;
  probability: number;
}

export interface Prediction {
  id: string;
  filename: string;
  /** Predicted tumor class (e.g. glioma, meningioma, pituitary, no_tumor) */
  predictedClass: string;
  /** Confidence score between 0 and 1 */
  confidence: number;
  probabilities: ClassProbability[];
  /** Data URL of the uploaded image for display */
  imageDataUrl: string;
  createdAt: string;
  modelVersion: string;
  processingTimeMs: number;
}

export interface ClassCount {
  label: string;
  count: number;
}

export interface PredictionStats {
  total: number;
  averageConfidence: number;
  tumorDetected: number;
  noTumor: number;
  byClass: ClassCount[];
  last24h: number;
}

export interface ModelInfo {
  name: string;
  version: string;
  classes: string[];
  inputSize: string;
  loaded: boolean;
  /** Inference backend (e.g. tensorflow-python, mock) */
  backend: string;
}
